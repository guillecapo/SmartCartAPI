# SmartCartAPI
Production-ready shopping cart API built with Hexagonal Architecture, DDD, and Spec-Driven Development. Features JWT auth, Redis + MongoDB dual persistence, RabbitMQ event-driven notifications, idempotency keys, auto-correction at checkout, and OpenAPI 3.0 spec via Swagger UI. Java 21 + Spring Boot 3.3.
